📡 Task 7 — Fetch & Display User Data Using the JavaScript Fetch API

This project demonstrates how to fetch and display data from a public API using Vanilla JavaScript.
It retrieves user information from the JSONPlaceholder API and displays it on the page in responsive, styled cards.

This project also includes error handling, a reload button, and a modern UI.

🚀 Features
✔ Fetch Data From Public API

Data is fetched from: https://jsonplaceholder.typicode.com/users


✔ Display User Info

Each user card shows:

Name

Email

Full Address (street, suite, city, zipcode)

✔ Reload Button

A Reload button allows refetching the data anytime.

✔ Error Handling

If the network fails or API is unavailable:

A clear error message appears

The UI updates without crashing

✔ Loading State

The status message updates to:

“Loading users…”

“Loaded X users”

Or an error message

✔ Modern UI Styling

The interface uses:

Gradient background

Card hover animations

Responsive grid layout

Clean, minimal text styling

🛠️ Technologies Used

HTML5

CSS3

JavaScript (Fetch API)

JSONPlaceholder Public API
